pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "WanAndroid-Kotlin"

include ':app'

include ':business:home'
include ':business:square'

include ':export:home'
include ':export:square'

include ':function:pay'
include ':function:share'

include ':common'

include ':base:network'
include ':base:image'
